import 'package:flutter/material.dart';

class WColors {
  WColors._();

  static const Color primary = Color(0xFFbc965e);

  // Background colors
  static const Color backgroundLight = Color(0xFFFAFAFA);
  static const Color backgroundDark = Color(0xFF1F1B16);

  // Container
  static const Color containerLight = Color(0xFFF0F0F0);
  static const Color containerDark = Color(0xFF272727);

  // Text colors

  static const Color textDark = Color.fromARGB(255, 0, 0, 0);
  static const Color textLight = Color.fromARGB(255, 253, 253, 253);

  // Button
  static const Color button = Color(0xFF1E1E1E);
  static const Color buttonLight = Color(0xFF1E1E1E);
}
